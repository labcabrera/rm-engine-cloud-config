configuration:
  projectName: rm-engine-ms-common

spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/rmengine

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8070/eureka/

server:
  port: 8090

management:
  context-path: /actuator
  security:
    enabled: false

logging:
  level.org: INFO
