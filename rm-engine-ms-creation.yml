configuration:
  projectName: rm-engine-ms-creation

spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/rmengine

server:
  port: 8093

management:
  context-path: /actuator
  security:
    enabled: false

logging:
  level.org: INFO
