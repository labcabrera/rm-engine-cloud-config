configuration:
  projectName: rm-engine-ms-manouver-dynamic

spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/rmengine

server:
  port: 8097

management:
  context-path: /actuator
  security:
    enabled: false

logging:
  level.org: INFO
